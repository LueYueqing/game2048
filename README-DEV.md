# 开发说明

## 🚀 **开发流程**

### **正确的开发方式：**
```bash
# 启动开发服务器（推荐）
npm run serve

# 或者监听构建
npm run dev
```

### **文件结构说明：**

- **`src/index.html`** - 开发时的 HTML 模板
- **`src/js/main.js`** - JavaScript 入口文件
- **`src/js/game.js`** - 游戏逻辑
- **`src/js/sound.js`** - 音效管理
- **`style.css`** - 样式文件（被 Webpack 处理）

### **构建输出：**
- **`dist/index.html`** - 生产版本（Vercel 部署用）
- **`dist/*.bundle.js`** - 打包后的 JavaScript
- **`dist/*.bundle.css`** - 打包后的 CSS

### **部署流程：**
1. 开发时使用 `npm run serve`
2. 构建时运行 `npm run vercel-build`
3. Vercel 自动部署 `dist/` 目录

## ⚠️ **注意事项**

- ❌ 不要直接修改 `dist/` 目录中的文件
- ❌ 不要使用根目录的 `index-original.html` 进行开发
- ✅ 开发时使用 `src/` 目录中的文件
- ✅ 使用 `npm run serve` 启动开发服务器
